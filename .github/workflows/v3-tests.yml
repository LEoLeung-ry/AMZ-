name: V3 regression tests

on:
  push:
    branches: [multisite-v3-beta]
  pull_request:
    branches: [main]

jobs:
  unit:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install dependencies
        run: python -m pip install -q --upgrade pip && pip install -q -r requirements.txt
      - name: Compile source
        run: python -m py_compile dashboard.py amz_intelligence/*.py
      - name: Run regression tests
        run: python -m unittest discover -s tests -p "test_*.py" -v

  live-source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Install dependencies
        run: python -m pip install -q --upgrade pip && pip install -q -r requirements.txt
      - name: Validate live Google Sheet sources
        run: python tests/live_source_smoke.py
