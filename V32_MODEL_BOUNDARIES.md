# V3.2 模型边界与使用原则

## 默认模式

- 默认使用“中性模式”。
- 不读取历史聊天中的目标品类，不给充电宝、剃须刀、垃圾袋、收纳等任何品类额外加分。
- 用户主动开启公司画像后，页面必须明确提示结果受到人工配置影响。

## 可验证数据层

来自四站公开表格：销售额、销量、搜索量、点击量、浏览量、平均价格、ASIN 数量、评分桶、热门关键词、类目 ID、名称和 Node Path（若源表提供）。

派生指标：单 ASIN 收入、单 ASIN 销量、搜索/ASIN、点击产出效率、点击率、站内百分位、人民币换算、证据字段覆盖和源指标重算一致性。

## 市场模型层

- 市场机会分属于人工设计的指标模型，不是成功概率。
- 默认排行榜使用五种标准策略的中位规则分，减少单一权重对结果的影响。
- 同时显示保守下限、乐观上限、策略一致度和进入 Top20% 的策略数量。
- 策略一致度只是权重敏感性，不是统计置信区间。

## 规则预警层

- A/B/C/D 是内部关键词和根类目风险预警，不是目标国法规结论。
- A 仅表示“未识别明显高门槛”，不等于已确认可售。
- B 表示存在常规条件，需要产品级复核。
- C/D 默认不进入普通候选榜，但仍保留在规则预警研究页。
- 每条记录显示触发规则、原因、可能需要的资源和“未做具体产品法规核验”的状态。

## 类目可操作性层

- Systems、Strips、Powders 等名称过宽的聚合节点，可能无法直接形成产品定义。
- 系统单独标记“名称过宽”和“层级偏浅”，不把它伪装成法规风险，也不改变 A/B/C/D 等级。
- 默认候选榜隐藏名称明显过宽的节点；用户可关闭筛选并查看 Node Path 后人工判断。
- 该层只解决“类目是否足够具体”，不判断利润、供应链或产品可行性。

## 金额边界

- 本位币保留，用于理解当地市场。
- 人民币用于跨站横向比较。
- 不把所有类目销售额直接相加，因为父子 Browse Node 可能重叠，汇总会重复计算。
- 总览改用中位数、P90、站内分位和单 ASIN 指标。
- 真实趋势必须使用本位币，避免汇率变化制造假增长。

## 当前无法回答的问题

仅凭现有类目汇总数据，不能可靠判断：采购成本、物流成本、广告成本、毛利、退货、库存周转、品牌集中度、专利、平台审核结果、具体产品法规准入和真实时间趋势。

## 停止继续加模型的边界

在没有新增证据前，不继续增加“利润分”“成功率”“真实趋势”“法律可售结论”等看似高级但无法验证的输出。后续只接受三类改动：修复程序错误、修复可复现的明显误判、提高结果透明度。除此之外继续叠加权重或规则，容易从正向优化变成过拟合和反向优化。
